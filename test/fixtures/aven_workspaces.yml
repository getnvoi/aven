# == Schema Information
#
# Table name: aven_workspaces
#
#  id          :bigint           not null, primary key
#  description :text
#  domain      :string
#  label       :string
#  slug        :string
#  created_at  :datetime         not null
#  updated_at  :datetime         not null
#
# Indexes
#
#  index_aven_workspaces_on_slug  (slug) UNIQUE
#
_fixture:
  model_class: Aven::Workspace

one:
  label: "Workspace One"
  description: "A test workspace"
  slug: "workspace-one"
  domain: "example.com"

two:
  label: "Workspace Two"
  description: "Another test workspace"
  slug: "workspace-two"
  domain: "example.org"
